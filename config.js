// Preencha depois de seguir o LEIA-ME.md.
// Com os dois campos vazios, o app roda em MODO DEMONSTRAÇÃO (salva só no aparelho).
window.APP_CONFIG = {
  // URL do "App da Web" do Google Apps Script (termina em /exec)
  API_URL: "",
  // ID do cliente OAuth do Google Cloud (termina em .apps.googleusercontent.com)
  CLIENT_ID: "",
};
